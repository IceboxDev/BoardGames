import type { GameModule } from "../types";
import accent from "./accent.json";

export default {
  slug: "connect-4",
  displayTitle: "Connect 4",
  bggId: 2719,
  accentHex: accent.hex,
} satisfies GameModule;
