/**
 * Hardware-mock slider — black inset rail with a textured "lock knob" on the
 * right. Purely decorative; matches `sky-team-lab/board.css` `.slider` 1:1.
 */
export default function CockpitSlider() {
  return <span aria-hidden="true" className="cockpit-slider" />;
}
