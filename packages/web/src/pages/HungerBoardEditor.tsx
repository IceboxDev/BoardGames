import BoardEditor from "../games/the-hunger/editor/BoardEditor";

// Dev-only: map The Hunger's printed board sides onto their images and
// publish them to the game. /dev/hunger-board
export default function HungerBoardEditor() {
  return <BoardEditor />;
}
