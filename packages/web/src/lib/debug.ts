// Visual layout-debug overlay — colored borders around board, fan, sidebar,
// and per-game pane splits. On in `vite dev`, stripped from production builds.
// Flip locally if you want dev without the borders.
export const DEBUG_LAYOUT = import.meta.env.DEV;
