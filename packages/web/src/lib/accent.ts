// The app accent as a hex literal, for the places that need a *value* rather
// than a class — SVG fills, chart strokes, `accentHex ?? …` fallbacks. This
// mirrors `--color-accent-500` in index.css; keep the two in sync. Before
// this constant existed, six files each hand-typed "#6366f1".
export const DEFAULT_ACCENT = "#6366f1";
